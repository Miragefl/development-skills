---
description: 小需求轻量开发流程（实现→自检→同步文档）
---
加载 flow skill（skill({ name: "flow" })），按它的轻量流程处理这个小需求：读 `PROJECT-CONTEXT.md` → 一句话复述 → 实现（遵循 `CODING-STANDARDS.md`）→ 自检跑 `test_cmd` → 动到接口/实体同步 doc。**不落盘规划文档**。

需求：$ARGUMENTS

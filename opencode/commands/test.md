---
description: 写测试 / TDD / 测试策略（单元·集成·边界）
---
加载 test skill（skill({ name: "test" })），按它的流程处理：定层（单元/集成/DAO）→ 先写失败测试（Red）→ 跑出失败 → 实现到通过（Green）→ 补边界 → 回归 → 全绿下重构。遵循 `TEST-STRATEGIES.md`。

目标：$ARGUMENTS
